const KEY='delegacjeFlotaDemoV2';
const seed={delegations:[],expenses:[],fuelings:[],incidents:[],notifications:[],vehicles:[{id:1,make:'Ford',model:'Focus',reg:'DEMO 001',mileage:125320,status:'available'},{id:2,make:'Skoda',model:'Octavia',reg:'DEMO 002',mileage:88420,status:'service'}]};
let state=JSON.parse(localStorage.getItem(KEY)||'null')||structuredClone(seed);for(const k of Object.keys(seed))if(!Array.isArray(state[k]))state[k]=structuredClone(seed[k]);
let view='home',selected=null;const app=document.getElementById('app'),cam=document.getElementById('cameraInput');
function save(){localStorage.setItem(KEY,JSON.stringify(state))}function esc(s=''){return String(s).replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]))}
function addNotification(text){state.notifications.unshift({id:Date.now()+Math.random(),text,created:Date.now()});save()}
function nav(active){return `<nav class="nav">${[['home','⌂','Start'],['delegations','🧳','Delegacje'],['fleet','🚗','Flota'],['profile','👤','Profil']].map(([v,i,l])=>`<button class="${active===v?'active':''}" onclick="go('${v}')">${i}<span>${l}</span></button>`).join('')}</nav>`}
function shell(body,active='home',title='Delegacje + Flota'){const n=state.notifications.length;app.innerHTML=`<div class="shell"><header class="top"><div><p class="eyebrow">${title}</p><h1>${active==='home'?'Dzień dobry':'Aplikacja mobilna'}</h1></div><button class="bell" onclick="go('notifications')">🔔${n?`<b>${n}</b>`:''}</button></header>${body}${nav(active)}</div>`}
function page(title,body){app.innerHTML=`<div class="shell"><header class="page"><button class="back" onclick="historyBack()">←</button><div class="grow"><p class="eyebrow">Delegacje + Flota</p><h1>${title}</h1></div></header><main>${body}</main></div>`}
function historyBack(){if(['delegation','expense'].includes(view))go('delegations');else if(['fuel','incident','vehicleNew'].includes(view))go('fleet');else go('home')}
function go(v,id=null){view=v;selected=id;render()}
function render(){({home,delegations,new:newDelegation,delegation:()=>delegation(selected),fleet,expense:()=>expense(selected),fuel:()=>fuel(selected),incident:()=>incident(selected),vehicleNew,notifications,profile}[view]||home)()}
function fmtGps(g){return g?`${g.lat.toFixed(5)}, ${g.lng.toFixed(5)} (±${g.accuracy}m)`:'brak'}
function getGps(){return new Promise((ok,no)=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>ok({lat:p.coords.latitude,lng:p.coords.longitude,accuracy:Math.round(p.coords.accuracy)}),no,{enableHighAccuracy:true,timeout:10000,maximumAge:0}):no(new Error('GPS niedostępny')))}
function home(){const active=state.delegations.find(d=>d.status==='started'),recent=[...state.delegations].reverse().slice(0,3),km=state.delegations.filter(d=>d.status==='finished').reduce((a,d)=>a+((d.odoEnd||0)-(d.odoStart||0)),0),cost=[...state.expenses,...state.fuelings].reduce((a,x)=>a+Number(x.amount||0),0),open=state.incidents.filter(x=>x.status!=='closed').length;shell(`<main><section class="hero"><p class="eyebrow" style="color:#d1d5db">Szybki start</p><h2>Nowa delegacja</h2><p>Pełny test mobilny: GPS, koszty, tankowania i flota.</p><button class="btn light" onclick="go('new')">+ Nowa delegacja</button></section><div class="stats"><div class="stat"><strong>${state.delegations.length}</strong><small>delegacje</small></div><div class="stat"><strong>${km}</strong><small>km</small></div><div class="stat"><strong>${cost.toFixed(2)}</strong><small>PLN kosztów</small></div><div class="stat"><strong>${open}</strong><small>usterki</small></div></div><section><div class="section-title"><h2>Aktualna delegacja</h2></div>${active?`<div class="card list" onclick="go('delegation',${active.id})"><div><span class="pill ok">w trasie</span><p><strong>${esc(active.destination)}</strong></p><p class="muted">${esc(active.number)}</p></div><b>›</b></div>`:`<div class="card empty"><div class="ico">🧳</div><div><strong>Brak aktywnej delegacji</strong><p>Rozpoczęty wyjazd pojawi się tutaj.</p></div></div>`}</section><section><div class="section-title"><h2>Szybkie akcje</h2></div><div class="grid"><button class="action" onclick="go('expense')"><span class="emoji">📷</span><strong>Paragon</strong><small>Dodaj dokument</small></button><button class="action" onclick="go('fuel')"><span class="emoji">⛽</span><strong>Tankowanie</strong><small>Litry i koszt</small></button><button class="action" onclick="go('fleet')"><span class="emoji">🚗</span><strong>Pojazdy</strong><small>Moja flota</small></button><button class="action" onclick="go('fleet')"><span class="emoji">⚠️</span><strong>Problem</strong><small>Zgłoś usterkę</small></button></div></section><section><div class="section-title"><h2>Ostatnie delegacje</h2><button onclick="go('delegations')">Wszystkie</button></div><div class="stack">${recent.length?recent.map(delegationCard).join(''):`<div class="card empty"><div class="ico">📋</div><div><strong>Brak danych</strong><p>Utwórz pierwszą delegację.</p></div></div>`}</div></section></main>`,'home')}
function delegationCard(d){return `<div class="card list" onclick="go('delegation',${d.id})"><div><strong>${esc(d.destination)}</strong><p class="muted">${esc(d.number)} · ${esc(d.from)} → ${esc(d.to)}</p></div><span class="pill ${d.status==='finished'?'ok':d.status==='started'?'warn':''}">${esc(d.status)}</span></div>`}
function delegations(){shell(`<main><div class="section-title"><h2>Moje delegacje</h2><button onclick="go('new')">+ Nowa</button></div><div class="stack">${state.delegations.length?[...state.delegations].reverse().map(delegationCard).join(''):`<div class="card empty"><div class="ico">🧳</div><div><strong>Brak delegacji</strong><p>Dodaj pierwszy wyjazd.</p></div></div>`}</div></main>`,'delegations')}
function newDelegation(){page('Nowa delegacja',`<form class="card form" onsubmit="createDelegation(event)"><label>Cel podróży<input name="destination" placeholder="np. Gliwice" required></label><label>Cel delegacji<textarea name="purpose" rows="4" required></textarea></label><div class="row2"><label>Od<input type="date" name="from" required></label><label>Do<input type="date" name="to" required></label></div><label>Transport<select name="transport"><option>Samochód służbowy</option><option>Samochód prywatny</option><option>Pociąg</option><option>Samolot</option></select></label><div class="notice">Wersja testowa zapisuje dane lokalnie na tym urządzeniu.</div><button class="btn dark full">Utwórz delegację</button></form>`)}
function createDelegation(e){e.preventDefault();const f=new FormData(e.target),id=Date.now();state.delegations.push({id,number:`DEMO/${new Date().getFullYear()}/${String(state.delegations.length+1).padStart(4,'0')}`,destination:f.get('destination'),purpose:f.get('purpose'),from:f.get('from'),to:f.get('to'),transport:f.get('transport'),status:'ready'});addNotification(`Utworzono delegację: ${f.get('destination')}`);save();go('delegation',id)}
function delegation(id){const d=state.delegations.find(x=>x.id===id);if(!d)return go('delegations');let actions='';if(d.status==='ready')actions=`<form class="card form" onsubmit="startTrip(event,${id})"><h2>Rozpocznij delegację</h2><label>Stan licznika<input type="number" name="odo" inputmode="numeric" required></label><label><input type="checkbox" name="gps" checked style="width:auto"> Zapisz lokalizację GPS</label><button class="btn dark full">▶ Rozpocznij</button></form>`;else if(d.status==='started')actions=`<div class="hero"><p class="eyebrow" style="color:#d1d5db">Delegacja trwa</p><h2>${esc(d.destination)}</h2><p>Start: ${new Date(d.start).toLocaleString('pl-PL')} · ${d.odoStart} km</p><small>GPS start: ${fmtGps(d.startGps)}</small></div><div class="grid"><button class="action" onclick="go('expense',${id})"><span class="emoji">📷</span><strong>Paragon</strong><small>Dodaj koszt</small></button><button class="action" onclick="go('fuel')"><span class="emoji">⛽</span><strong>Tankowanie</strong><small>Dodaj paliwo</small></button></div><form class="card form" onsubmit="finishTrip(event,${id})"><h2>Zakończ delegację</h2><label>Stan licznika końcowy<input type="number" name="odo" min="${d.odoStart}" required></label><label><input type="checkbox" name="gps" checked style="width:auto"> Zapisz lokalizację GPS</label><button class="btn dark full">■ Zakończ</button></form>`;else actions=`<div class="notice ok">Delegacja zakończona. Przebieg: ${(d.odoEnd||0)-(d.odoStart||0)} km<br>GPS start: ${fmtGps(d.startGps)}<br>GPS koniec: ${fmtGps(d.endGps)}</div><button class="btn dark full" onclick="go('expense',${id})">Dodaj koszt / paragon</button>`;page(d.destination,`<div class="card detail"><div><span>Numer</span><strong>${esc(d.number)}</strong></div><div><span>Status</span><strong>${esc(d.status)}</strong></div><div><span>Termin</span><strong>${esc(d.from)} → ${esc(d.to)}</strong></div><div><span>Cel</span><strong>${esc(d.purpose)}</strong></div></div>${actions}`)}
async function startTrip(e,id){e.preventDefault();const d=state.delegations.find(x=>x.id===id),f=new FormData(e.target);let pos=null;if(f.get('gps'))try{pos=await getGps()}catch{alert('Nie udało się pobrać GPS. Delegacja zostanie rozpoczęta bez lokalizacji.')}d.odoStart=Number(f.get('odo'));d.start=Date.now();d.startGps=pos;d.status='started';addNotification(`Rozpoczęto delegację: ${d.destination}`);save();render()}
async function finishTrip(e,id){e.preventDefault();const d=state.delegations.find(x=>x.id===id),f=new FormData(e.target),odo=Number(f.get('odo'));if(odo<d.odoStart)return alert('Stan końcowy nie może być niższy.');let pos=null;if(f.get('gps'))try{pos=await getGps()}catch{alert('Nie udało się pobrać GPS. Zapisuję bez lokalizacji.')}d.odoEnd=odo;d.end=Date.now();d.endGps=pos;d.status='finished';addNotification(`Zakończono delegację: ${d.destination}`);save();render()}
function fleet(){shell(`<main><div class="section-title"><h2>Pojazdy</h2><button onclick="go('vehicleNew')">+ Dodaj</button></div><div class="stack">${state.vehicles.map(v=>`<div class="card"><div class="list"><div><strong>${esc(v.make)} ${esc(v.model)}</strong><p class="muted">${esc(v.reg)} · ${Number(v.mileage).toLocaleString('pl-PL')} km</p></div><span class="pill ${v.status==='service'||v.status==='unsafe'?'danger':'ok'}">${esc(v.status)}</span></div><div class="vehicle-actions"><button onclick="go('fuel',${v.id})">⛽ Tankowanie</button><button onclick="go('incident',${v.id})">⚠️ Problem</button></div></div>`).join('')}</div></main>`,'fleet')}
function vehicleNew(){page('Dodaj pojazd',`<form class="card form" onsubmit="saveVehicle(event)"><div class="row2"><label>Marka<input name="make" required></label><label>Model<input name="model" required></label></div><label>Nr rejestracyjny<input name="reg" required></label><label>Stan licznika<input type="number" name="mileage" min="0" required></label><button class="btn dark full">Dodaj pojazd</button></form>`)}
function saveVehicle(e){e.preventDefault();const f=new FormData(e.target);state.vehicles.push({id:Date.now(),make:f.get('make'),model:f.get('model'),reg:f.get('reg'),mileage:Number(f.get('mileage')),status:'available'});save();go('fleet')}
function expense(delegationId=null){page('Dodaj wydatek',`<form class="card form" onsubmit="saveExpense(event,${delegationId||'null'})"><label>Kategoria<select name="cat"><option>Nocleg</option><option>Parking</option><option>Opłata drogowa</option><option>Posiłek</option><option>Inne</option></select></label><label>Kwota PLN<input type="number" step="0.01" min="0.01" name="amount" required></label><label>Opis<textarea name="desc" rows="3"></textarea></label><button type="button" class="btn light" style="border:1px solid #ddd" onclick="cam.click()">📷 Zrób zdjęcie paragonu</button><div id="fileInfo" class="file-preview"></div><button class="btn dark full">Zapisz wydatek</button></form>`)}
cam.onchange=()=>{const f=cam.files[0],el=document.getElementById('fileInfo');if(el)el.textContent=f?`Wybrano: ${f.name} (${Math.round(f.size/1024)} KB)`:''}
function saveExpense(e,delegationId){e.preventDefault();const f=new FormData(e.target),file=cam.files[0];state.expenses.push({id:Date.now(),delegationId,cat:f.get('cat'),amount:Number(f.get('amount')),desc:f.get('desc'),document:file?{name:file.name,size:file.size,type:file.type}:null,created:Date.now()});addNotification('Dodano koszt do rozliczenia');save();cam.value='';go('home')}
function fuel(vehicleId=null){const options=state.vehicles.map(v=>`<option value="${v.id}" ${vehicleId===v.id?'selected':''}>${esc(v.make)} ${esc(v.model)} — ${esc(v.reg)}</option>`).join('');page('Tankowanie',`<form class="card form" onsubmit="saveFuel(event)"><label>Pojazd<select name="vehicleId">${options}</select></label><div class="row2"><label>Litry<input type="number" name="liters" step="0.01" min="0.01" required></label><label>Kwota PLN<input type="number" name="amount" step="0.01" min="0.01" required></label></div><label>Stan licznika<input type="number" name="mileage" min="0" required></label><label>Stacja<input name="station" placeholder="np. Orlen"></label><button type="button" class="btn light" style="border:1px solid #ddd" onclick="cam.click()">📷 Zdjęcie faktury/paragonu</button><div id="fileInfo" class="file-preview"></div><button class="btn dark full">Zapisz tankowanie</button></form>`)}
function saveFuel(e){e.preventDefault();const f=new FormData(e.target),v=state.vehicles.find(x=>String(x.id)===String(f.get('vehicleId'))),m=Number(f.get('mileage'));if(v&&m<Number(v.mileage))return alert('Nowy przebieg nie może być niższy niż obecny.');if(v)v.mileage=m;const file=cam.files[0];state.fuelings.push({id:Date.now(),vehicleId:v?.id,liters:Number(f.get('liters')),amount:Number(f.get('amount')),mileage:m,station:f.get('station'),document:file?{name:file.name,size:file.size,type:file.type}:null,created:Date.now()});addNotification(`Dodano tankowanie: ${v?.reg||''}`);save();cam.value='';go('fleet')}
function incident(vehicleId){const v=state.vehicles.find(x=>x.id===vehicleId);page('Zgłoś problem',`<form class="card form" onsubmit="saveIncident(event,${vehicleId})"><div class="notice">${esc(v?.make||'')} ${esc(v?.model||'')} ${esc(v?.reg||'')}</div><label>Kategoria<select name="cat"><option>Awaria</option><option>Szkoda</option><option>Opony</option><option>Kontrolka</option><option>Serwis</option><option>Dokumenty</option><option>Inne</option></select></label><label>Opis<textarea name="desc" rows="5" required></textarea></label><label><input type="checkbox" name="unsafe" style="width:auto"> Pojazd nie powinien dalej jechać</label><button class="btn dark full">Wyślij zgłoszenie</button></form>`)}
function saveIncident(e,vehicleId){e.preventDefault();const f=new FormData(e.target),unsafe=f.get('unsafe')==='on',v=state.vehicles.find(x=>x.id===vehicleId);state.incidents.push({id:Date.now(),vehicleId,cat:f.get('cat'),desc:f.get('desc'),unsafe,status:'open',created:Date.now()});if(v&&unsafe)v.status='unsafe';addNotification(`Nowe zgłoszenie floty: ${f.get('cat')}`);save();go('fleet')}
function notifications(){page('Powiadomienia',`${state.notifications.length?`<div class="stack">${state.notifications.map(n=>`<div class="card"><strong>${esc(n.text)}</strong><p class="muted">${new Date(n.created).toLocaleString('pl-PL')}</p></div>`).join('')}</div>`:`<div class="card empty"><div class="ico">🔔</div><div><strong>Brak powiadomień</strong></div></div>`}<button class="btn light full" style="border:1px solid #ddd;margin-top:14px" onclick="clearNotifications()">Wyczyść powiadomienia</button>`)}
function clearNotifications(){state.notifications=[];save();render()}
function profile(){const km=state.delegations.filter(d=>d.status==='finished').reduce((a,d)=>a+((d.odoEnd||0)-(d.odoStart||0)),0);shell(`<main><div class="stats"><div class="stat"><strong>${state.delegations.length}</strong><small>delegacje</small></div><div class="stat"><strong>${km}</strong><small>km</small></div><div class="stat"><strong>${state.expenses.length+state.fuelings.length}</strong><small>koszty</small></div><div class="stat"><strong>${state.vehicles.length}</strong><small>pojazdy</small></div></div><div class="card"><strong>Tryb testowy PWA</strong><p class="muted">Dane są zapisane w pamięci lokalnej tego telefonu.</p><button class="btn danger full" onclick="resetDemo()">Wyczyść dane testowe</button></div></main>`,'profile')}
function resetDemo(){if(confirm('Usunąć dane testowe?')){localStorage.removeItem(KEY);state=structuredClone(seed);go('home')}}
if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js'));
render();